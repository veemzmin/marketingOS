---
phase: 01-foundation-and-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.js
  - tailwind.config.js
  - .env.example
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Developer can run `npm run dev` and see Next.js app at localhost:3000"
    - "TypeScript compilation succeeds without errors"
    - "Tailwind classes apply correctly to components"
  artifacts:
    - path: "package.json"
      provides: "Dependencies for Next.js 14, TypeScript, Tailwind, React"
      contains: "next"
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict mode"
      contains: "strict"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Tailwind imports"
      min_lines: 15
  key_links:
    - from: "src/app/layout.tsx"
      to: "globals.css"
      via: "import statement"
      pattern: "import.*globals\\.css"
---

<objective>
Initialize Next.js 14 project with TypeScript, Tailwind CSS, and development tooling configured for App Router patterns.

Purpose: Establish the foundational web application structure that all auth and multi-tenant features will build upon.
Output: Runnable Next.js development server with TypeScript compilation and Tailwind styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md

# Stack decisions
- Next.js 14 (App Router)
- TypeScript (strict mode)
- Tailwind CSS for styling
- Solo developer, prioritize simple tooling
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js 14 project with TypeScript</name>
  <files>package.json, tsconfig.json, next.config.js, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-git`
    2. Accept prompts: TypeScript=yes, Tailwind=yes, App Router=yes, src/ directory=yes
    3. Verify package.json includes: next@^14, react@^18, typescript@^5
    4. Update tsconfig.json to enable strict mode: `"strict": true`
    5. Create .env.example with placeholder variables:
       ```
       DATABASE_URL="postgresql://user:password@localhost:5432/marketing_os"
       NEXTAUTH_URL="http://localhost:3000"
       NEXTAUTH_SECRET="replace-with-random-secret-in-production"
       ```
    6. Add .env to .gitignore if not already present
  </action>
  <verify>
    Run `npm run dev` and visit http://localhost:3000 - should see default Next.js landing page.
    Run `npm run build` - should compile without TypeScript errors.
  </verify>
  <done>
    Next.js 14 project initialized with TypeScript strict mode, Tailwind CSS configured, development server runs successfully.
  </done>
</task>

<task type="auto">
  <name>Configure ESLint and development tooling</name>
  <files>.eslintrc.json, package.json</files>
  <action>
    1. Verify .eslintrc.json exists (created by create-next-app)
    2. Add recommended rules for healthcare/compliance context:
       ```json
       {
         "extends": "next/core-web-vitals",
         "rules": {
           "no-console": ["warn", { "allow": ["warn", "error"] }],
           "@typescript-eslint/no-explicit-any": "error",
           "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }]
         }
       }
       ```
    3. Install Prettier for consistent formatting: `npm install -D prettier eslint-config-prettier`
    4. Create .prettierrc:
       ```json
       {
         "semi": false,
         "singleQuote": false,
         "tabWidth": 2,
         "trailingComma": "es5"
       }
       ```
    5. Add format script to package.json: `"format": "prettier --write \"src/**/*.{ts,tsx,js,jsx,json,css}\""`
  </action>
  <verify>
    Run `npm run lint` - should pass without errors.
    Run `npm run format` - should format files successfully.
  </verify>
  <done>
    ESLint configured with strict TypeScript rules, Prettier installed for consistent code formatting.
  </done>
</task>

<task type="auto">
  <name>Create basic landing page with health check</name>
  <files>src/app/page.tsx, src/app/api/health/route.ts</files>
  <action>
    1. Replace src/app/page.tsx with minimal landing page:
       ```tsx
       export default function HomePage() {
         return (
           <main className="flex min-h-screen items-center justify-center">
             <div className="text-center">
               <h1 className="text-4xl font-bold mb-4">Marketing OS</h1>
               <p className="text-gray-600">Governance-first content operations platform</p>
             </div>
           </main>
         )
       }
       ```
    2. Create src/app/api/health/route.ts for deployment health checks:
       ```ts
       import { NextResponse } from "next/server"

       export async function GET() {
         return NextResponse.json({
           status: "ok",
           timestamp: new Date().toISOString()
         })
       }
       ```
    3. Test health endpoint: curl http://localhost:3000/api/health should return JSON with status: "ok"
  </action>
  <verify>
    Visit http://localhost:3000 - should see "Marketing OS" heading with Tailwind styles applied.
    Visit http://localhost:3000/api/health - should return JSON: {"status":"ok","timestamp":"..."}
  </verify>
  <done>
    Landing page displays with Tailwind styling, API health check endpoint responds successfully.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts development server without errors
2. `npm run build` compiles TypeScript without errors
3. `npm run lint` passes ESLint checks
4. Visiting localhost:3000 shows landing page with Tailwind styles
5. Health check endpoint returns JSON successfully
</verification>

<success_criteria>
- Next.js 14 project runs in development mode
- TypeScript strict mode enabled and compiling
- Tailwind CSS applies styles correctly
- ESLint and Prettier configured
- .env.example documents required environment variables
- Health check API endpoint functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md`
</output>
